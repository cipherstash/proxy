


[tasks.test]
alias = 't'
description = "All test"
run = 'cargo nextest run --no-fail-fast --nocapture {{arg(name="test",default="")}}'


#MISE description="Build the CLI"
# [tasks.up]
# run = "docker compose up --build --detach"
#     # sanity check direct connection
#     psql postgresql://mlp:password@localhost:5432/mlp

#     # proxy connection
#     psql postgresql://mlp:password@localhost:6432/mlp

# up:
#     #!/usr/bin/env bash
#     docker compose up --build --detach

# connect:
#     #!/usr/bin/env bash
